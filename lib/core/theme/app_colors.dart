import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette
  static const Color primary = Color(0xFF1A1A1A); // Almost Black
  static const Color primaryLight = Color(0xFF424242); 
  
  // Secondary Palette
  static const Color secondary = Color(0xFFC6A969); // Gold/Brass (Logo)
  
  // Card Accents (from Image 2)
  static const Color cardEspresso = Color(0xFFFFB48F); // Salmon/Orange
  static const Color cardFilter = Color(0xFF9FE2BF); // Mint Green
  static const Color cardHistory = Color(0xFFFFFFFF); // White
  
  // Backgrounds
  static const Color background = Color(0xFFF2F0E9); // Vintage Cream
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFE6E4DD);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF595959);
  
  // Functional
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
}
